# ============================================================================
# ArsenalsLOS 顶层 .gitignore —— 仅跟踪用户自定义内容
# ----------------------------------------------------------------------------
# 本 git 仓库与 `repo` 工具各司其职：
#   - Android/LineageOS 上游源码树（.repo/ + 1164 个 git 子项目）由 repo 管理，
#     绝不纳入此 git。
#   - 此处仅跟踪用户手写/自定义的顶层文件（CLAUDE.md 等）。
# 采用白名单：先忽略一切 (*)，再用 ! 放行特定文件/目录。
# ============================================================================

# --- 白名单基线：忽略一切 ---
*

# --- 始终跟踪的元文件 ---
!.gitignore
!.gitattributes
!CLAUDE.md
!README.md

# --- 按需放行自定义内容（取消注释并改为你的路径）---
# !local_manifests/
# !custom_patches/
# !custom_scripts/
# !device/<your_vendor>/
# !vendor/<your_vendor>/

# --- 编译输出与 repo 元数据（双重保护，即便上方误放行也不跟踪）---
/out/
/.repo/
